# STATE — what is true right now

**Last verified: 23 Jul 2026 (session — #155 + #176 SHIPPED to main + deployed via Vercel, pending owner live-verify; #173 bulk restock also already on main + owner-closed). #155: clearAllSessions server-first (RPC `clear_club_sessions()` already applied+probed in prod) merged to main — owner live-verify open (internet-on wipes both sides, internet-off aborts clean, no resurrection, no orphan session_items, staff-JWT owner-gate rejection on-device). #176: `CanteenItem.category` (drinks/cigarettes/snacks/other) synced both ways (push null-clears, pull LENIENT), prod column already live (text/nullable/no-CHECK — probe-verified, NO migration); `listRestockItems()` re-sorted (categoryRank, name) restock-surface-only, existing NULL items sort last; Chunk 2 shared `CategoryPicker`, form add/edit + one-time bulk-tag screen `/canteen/tag-categories` owner-only, `updateCanteenItem` clear-path sends NULL via `'category' in patch`. Both #155+#176 merged into main this session (two `--no-ff` merges, one changelog conflict resolved). Owner POST-TAG on-device re-verify of row-N-with-real-categories is #176's close gate. (Separately, `feat/175-rollup-spike` parked with Chunk 0 rollup plumbing — v24, not merged.) — prior 22 Jul session context: Ball Bended recovery + #169/#170/#171 shipped to main. Owner Google (`ballbended@gmail.com`) disabled by Google; recovered the club's data via a temp uncommitted staff-export (reverted, NOT shipped), stood up new owner `ballbended01@gmail.com`, repointed its `users_meta.club_id` to the real Ball Bended club (`1eec5dc8…`, #160 pattern) so staff (Naresh) + data flow to it. #169: reverted staff canteen edit/delete/restock → owner-only (staff was overwriting stock counts; restock hard-blocked via 20260722_revert_staff_restock_rls, edit/delete UI-gate only). #170: staff can now read the clubs row (20260722_clubs_staff_select_rls) so the UPI QR hydrates on staff devices. Both RLS migrations owner-run + MCP-probe-verified; PUSHED to main this session. #171: owner is a silent 5-min backup for table alarms (role-scoped in useSessionAlarm, no schema) — pushed to main. Ball Bended trial extended to 22 Sep (ballbended01 sub, drives the RPC gate for owner + Naresh). — prior 21 Jul CLI session context follows: #165 + #166 + #167 + #168 ALL code done, NOT yet pushed (owner batch-deploy), pending owner live-verify. #168: two concurrent RUNNING sessions on one table (header count > cards + dup active rows in History) — Invariant #1 was UI-only-guarded, breaks across devices under LWW sync; fixed at 2 layers (startSession() `TableBusyError` block + `reconcileActiveSessions()` self-heal keeping earliest via `compareSessionCanonical`), stock-return extracted to shared `returnSessionItemStock`, active readers now filter `!deletedAt`, header counts unique tables. New Pattern T11. #165: walk-in Quick Sales render + total in History (canteenSales was never read there — 3rd recurrence of the #93/#141 omission; promoted Pattern S30). #166: `reverseCanteenSale` (S29 twin) reverses a walk-in from History under the Edit-history toggle + CLOSED the S30 soft-delete gap — every canteenSales money reader now filters `!deletedAt` (also caught a #162 Piggy-cash-by-week sessions residual). #167: Canteen + Quick Sale search boxes + shared `CanteenItemPicker` extracted (AddItem/QuickSale/BackEntry; Canteen mgmt page deliberately not migrated). Prior context: Rules L + M added 19 Jul; #156/#157/#158/#153/#154 closed; #155 (clearAllSessions) still open, code not started. #128 CLOSED (Phase D) AND Phase C sync tail CLOSED — sync architecture complete end-to-end. Focus = code queue: ZERO P0s → #155 → #142–#144 decision. #127 owner E2E of P1c–P2 still its close gate.).**
Rules for this file: OVERWRITE in place, never append (Rule G lives here). One line per module. No commit SHAs, no build sizes, no dates inside status lines — history belongs in `references/history/changelog.md` + `git log`. Pending entries are deleted the moment they resolve. **claude.ai sessions:** if the stamp above is more than ~7 days old, say so to Sugeet and trust GitHub/his answers over this file.

## Current focus

**Phase D + Phase C sync tail both CLOSED by owner (19 Jul)** — sync architecture (write/read/backfill/RLS/actor-stamping/staff roles) COMPLETE end-to-end, no open build work; deferred staff ENHs live on as #136 (owner-set passwords) + #137 (permission tiers). **Current focus = the code queue.** THIS SESSION (21 Jul, NOT yet pushed — owner batch-deploy pending): **#165 + #166 + #167 + #168 all CODE DONE, pending owner live-verify** — walk-in Quick Sales in History (#165), reverse a walk-in from History + full canteenSales `!deletedAt` coverage (#166), Canteen/QuickSale search + shared `CanteenItemPicker` (#167), one-active-session-per-table durable enforcement + cross-device self-heal (#168, Pattern T11). **Also queued:** #173 (bulk restock — all 6 chunks CODE DONE, owner print-preview verify pending, NOT committed), #175 (Canteen+Table insight pages — FILED, not started, owner wants weeks of real restock usage first; deferred margin/expense metrics flagged to a future costPrice/expenses workstream). #172 (AI restock photo-parser) SHELVED, issue retained. **Priority order for what's LEFT:** **ZERO open P0s** (#103/#100 closed), #145 (fresh-device booking-config hydration — CODE DONE, awaiting fresh-device verify), #155 (clearAllSessions server-first — SHIPPED to main + deployed 23 Jul, pending owner live-verify: internet-on clear wipes both sides, internet-off aborts cleanly, no resurrection on fresh pull, no orphan session_items, staff-JWT owner-gate rejection on-device), #142/#143/#144 (swallowed-MirrorResult contract decision), #64/#123 (P2 sync-UX polish), player-identity quartet #149–#152 (deferred track). Legacy Phase D notes: staff matrix amended (staff keep back-entry creation + can create customers); D8b resolved via #157 usernames.


## Module status (one line each — overwrite in place)

- **Sync (Phase C)** — Write path (outbox + SyncRunner on lock-free `supabaseSync`) + read path (SyncReader: serialized queue, direct-apply LWW on epoch-ms, 4 realtime channel groups, 30s-grace/60s polling fallback) LIVE across all 9 tables; write-site cutover 100% (#122/#126/#124 — incl. session-item soft-delete + `!deletedAt` filters on every session_items reader), runtime-proven (delete/undo/ghost-pull rounds, outbox 0); **Phase C CLOSED — #110/#112/#125/#126 owner-verified.** One-time boot backfill (#129) uploads pre-Phase-C rows via the outbox (`ignoreDuplicates:true` → ON CONFLICT DO NOTHING, required for append-only wallet_transactions); prod mirrors local across all 9 tables (runtime-proven). Contract: `ripple_effects.md` §Sync + Patterns S14–S24.
- **Auth + cardless trial** — Supabase Google OAuth (`select_account`), 7-day cardless trial via Postgres trigger (staff excluded via `ck_role` marker), `subscriptionLoaded` race guard, stranded-lock degraded boot (Pattern A11; #120 owner-verified + closed); staff admin endpoints `api/create-staff.ts` + `api/manage-staff.ts` (D2) + client staff login (D3: `authStore.role` from the JWT claim via `useRole`, collapsed staff sign-in on Signup, staff gate on the owner's subscription via `get_club_subscription_status()` RPC, staff renew-card on Subscribe) + claim-gated demo seed and staff-only Account card in Settings (D4) + operations role gates via `RoleGuard` `<OwnerOnly>`/`<HideForStaff>` — SessionDetail edit/move, History staff = back-entry card only, Home Add-Table FAB (D5, Pattern A12) + commerce role gates — Canteen item **add** STAFF-ALLOWED; **edit/delete/restock REVERTED to owner-only (#169, 22 Jul)** after staff silently overwrote stock counts (edit/delete = UI-gate only since canteen_items UPDATE RLS stays open for the #131 session-decrement path; restock hard-blocked via 20260722_revert_staff_restock_rls restoring stock_purchases to owner-only), bulk-peak STAYS owner-only; **staff clubs-row READ added (#170, 22 Jul, 20260722_clubs_staff_select_rls) so the UPI QR hydrates on staff devices** (clubs SELECT was owner-only → getOwnerClub() NULL → no QR); CustomerProfile adjust/edit, Piggy role split; Wallet/WalletNewCustomer/WalletTopup/QuickSale deliberately ungated (D6). Gate map: `ripple_effects.md` §Roles. Staff commerce writes E2E-verified post-RLS-fix (#130/#131 closed); #132 exclusion-list gap also fixed + E2E-proven (staff `reference_type='manual'` now 403, owner adjustment unaffected). Route-level gates via `RequireOwner` (`/piggy` bounces staff to `/tables`) + staff today-only Summary card, BottomNav unchanged (D7). Owner-only Settings → Staff management (D8: `StaffSection` — list/create/reset/revoke via D2 endpoints, show-once creds, 12s list-timeout guard; #157 permanent usernames via `users_meta.username` + copy-login button; #158 removed staff as compact record lines; #156 reset purges removed staff). Full route table + matrix: `ripple_effects.md` §Roles.
- **Subscription (Razorpay)** — LIVE mode in production, NACH auto-debit collecting ₹599; V1-LAUNCH shows Standard Monthly only; serverless create/webhook/cancel.
- **Advance booking (#84/#106/#127/#147)** — Owner side + per-club hours + per-30-min-slot advance shipped; pending-hold v2 LIVE + owner-verified (#147 closed: 10-min lazy expiry, per-slot `status` → amber Pending chips, guarded `confirm_booking_intent` with typed `intent_expired`/`slot_taken`, future-slot cleanup fix); both booking migrations applied in prod; #127 client fix landed (table-id `number`→`string`), only owner E2E of P1c–P2 left before close. #148 (code-match polish) open, code not started.
- **Player Hub + topups** — Live: slug setup, `/c/:slug` UPI topup flow with polling, realtime inbox + badge, `/poster/:slug` auto-print.
- **Pricing visibility** — Player-side collapsible pricing card, gated on `acceptsPricingDisplay` + populated `tables_json`.
- **ClubCoins** — Off by default; tiered earn on topup, configurable redemption, FIFO expiry sweep every 4h.
- **Engagement** — Welcome bonus, streak bonus, dormancy nudges, BringBackList — all off by default.
- **Wallet / prepaid credit** — Customers, ledger, top-ups, adjustments, walk-in codes, WhatsApp receipts; refund UI still pending (Phase 3).
- **Canteen + POS stock sync** — Item CRUD, stock pills, RestockSheet; all add/mutate paths sync stock atomically (D7/S24 discipline). Canteen page has a search box (#167); mgmt cards keep their own list (NOT the shared picker — they're edit/delete/restock, not add).
- **Bulk restock (#173, owner-only, SHIPPED to main + deployed, owner print-tested, CLOSED)** — offline NumberPad-driven bulk entry (`/canteen/bulk-restock`, no OS keyboard), debounced draft with explicit Resume/Start-fresh (R6a, device-local via `restockDraft.ts`, NOT synced), confirm sheet with double-tap guard. Batch history + reverse (`/canteen/restock-history`): batches from `stockPurchases.notes` (`bulk-entry`/`bulk-reversal <batchId> <ISO>`), reverse writes compensating `kind='reversal'` rows (`currentStock -= qty` clamped ≥0, never hard-delete, idempotent). `StockPurchase.kind`/`reason` synced BOTH ways (#174, owner-run + probe-verified). Printable A4 sheet: `listRestockItems()` is the SHARED ordering both screen + sheet use (R1, screen row N === paper row N), lazy-jspdf `downloadRestockSheet()` → real .pdf. RequireOwner route gate.
- **Canteen category (#176, owner-only, SHIPPED to main + deployed, pending owner verify)** — `CanteenItem.category` (drinks/cigarettes/snacks/other, extensible free-text underneath) synced BOTH ways (push explicit-null-clears; pull LENIENT not reqEnum so a future value doesn't dead-letter old clients). Prod column already live (text/nullable/no-CHECK — probe-verified, NO migration). `listRestockItems()` sorts (categoryRank, name, sortOrder-tiebreak) — restock surface ONLY; `getCanteenItems()` unchanged (sell order intact). Existing NULL items sort LAST until categorised. Row N === paper N preserved (single shared ordering). Chunk 2: shared `CategoryPicker` (chips from `CATEGORY_LABELS`, tap-selected-to-clear) used by BOTH `CanteenItemFormModal` (add/edit) and the one-time bulk-tag screen (`BulkCategoryTag`, `/canteen/tag-categories`, RequireOwner, inline chips + one Save on dirty rows). `updateCanteenItem` clear-path: `'category' in patch` sends real NULL (Omit-then-add nullable generic). Owner's close gate: tag items on-device, regen PDF, re-verify row N with non-uniform categories.
- **Shared canteen picker (#167)** — `CanteenItemPicker` (searchable 2/3-col tap-grid, out-of-stock blocked, peak-aware, ×N badge, optional stock pill) backs the 3 tap-to-add surfaces: AddItem (session), QuickSale (`showStock`), BackEntry (`usePeakPricing=false`). One component → changes ripple to all 3.
- **Low-stock threshold (#92)** — Owner-configurable 1–999 (default 5) in Settings → Canteen; all surfaces via `getLowStockThreshold()`.
- **Peak pricing (#68)** — Verified; all 4 phases live; canteen-only; QuickSale cart captures price at tap.
- **Quick Sale** — Walk-in canteen sales with PaymentSplitSheet; atomic sale+stock+wallet tx. Items grid is the shared `CanteenItemPicker` (#167).
- **History** — Owner past-session list + CSV + #162 Edit-history toggle. #165: reads `canteenSales` (`useCanteenSalesInRange`) so walk-in sales render + count in day totals + CSV; table-filter excludes them. #166: under the toggle, a walk-in row is tappable → `reverseCanteenSale` (returns stock + reverses wallet leg, S29 twin) via confirm modal. Every canteenSales money reader now filters `!deletedAt` (S30 gap closed).
- **Split payments + Piggy** — `paymentBreakdown` mandatory capture, PAYMENT MODE + CASH FLOW strips, derived piggy balance, `/piggy` page.
- **Sessions (invariant)** — One active session per table (#168, Pattern T11): `startSession()` blocks a 2nd via `TableBusyError`; `reconcileActiveSessions()` self-heals cross-device LWW dups (keeps `compareSessionCanonical` earliest, tombstones losers + returns their stock via shared `returnSessionItemStock`); `Home` fires it on dup detect + counts unique running tables; all active readers filter `!deletedAt`.
- **Table Move** — Running/paused session to empty same-type same-rate table; single continuous bill.
- **Back entries** — Paper-notebook backfill with canteen items, overlap + stock checks; `per_frame` excluded.
- **Rate card + tolerance billing** — Per-table tiers + tolerance + `minimum|prorated`; snapshots at start; rounding ignored on rate-card sessions.
- **Alarm / notify-at** — Per-session wall-clock alarm; snooze anchors to ORIGINAL fire time (Pattern T6). #171: owner is a SILENT BACKUP — `useSessionAlarm(sessions, role)` withholds the alarm on owner devices for 5 min past `notifyAtMs`, then escalates if unacknowledged; staff fires immediately. Shared session field, NOT siloed (never make it staff-only).
- **Summary dashboard** — End-of-day dashboard incl. Quick Sale in all aggregates (Pattern T9).
- **Settings** — Collapsible sections (one open); SaveIndicator on every save site (U10); clubs-row mirrors via `mirrorToSupabaseBySlug` (S11). #155: "Clear session history" is server-first — `clearClubSessionsRemote()` RPC runs first, Dexie `clearAllSessions()` (soft-deletes sessions + session_items) mirrors only on success; abort+toast on failure, same PH2 idiom as `handleReset`. SHIPPED to main + deployed, pending owner live-verify.
- **Import / Export** — Atomic full-DB export/import, `ClubKeeperBackupV21`, 7 typed failure reasons, DEV round-trip self-test.
- **Desktop responsiveness (#91)** — Verified for Tables/Canteen/Bookings/QuickSale/shared Modal/PaymentSplitSheet; Settings + Wallet-topup-success still mobile-only.
- **PWA + deployment** — Vercel auto-deploy from `main`, SPA rewrite, per-user IndexedDB `ClubKeeperDB_<userId>`, custom domain `app.handbookhq.in`.
- **Dexie schema** — **v23 current** (`CURRENT_SCHEMA_VERSION = 23`); v23 = additive `restockDrafts` store (#173, device-local NOT-synced draft); v22 = additive index (`walletTransactions.referenceId`, #162/#163); v20 = UUID migration. All additive, no `.upgrade()`. #176 adds `canteen_items.category` (Dexie field only — NOT indexed, rides existing store, no version bump; prod column already live text/nullable/no-CHECK, no migration). (Note: `feat/175-rollup-spike` branch bumps to v24 with a `rollups` store — not on main yet.) Details in `references/data_model.md`.
- **Bug tracking** — GitHub issues ONLY authoritative; `history/bug_archive.md` is the offline pointer index.

## Load-bearing pending (blocks something; delete when resolved)

- **Vercel webhook config** — Razorpay Dashboard → add `/api/razorpay-webhook` URL + `RAZORPAY_WEBHOOK_SECRET` env in Vercel → redeploy. Until done, subscription status updates rely on the frontend's optimistic refresh only.
- **PWA update banner** — needs `useRegisterSW` + banner UI; without it, users on an old service worker miss new deploys without a hard refresh (root cause of recurring Pattern W1 sessions).
- **Wallet Phase 3 (refund UI)** — `referenceType: 'refund'` + mandatory notes; until built, refunds are manual adjustments.
- **PAYMENT MODE v13 backfill gap** — `paymentBreakdown.cash` understates pre-v13 sessions by items value; defer until Ball Bender notices.
- **Session persistence watch** — `storage` option was linter-removed from `createClient`; monitor for session drops in production.
- **Razorpay key rotation warning** — any rotation of `VITE_RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET` requires re-verifying all 6 plan IDs (Pattern S5 curl check).
- **GST invoicing + email notifications** — next sprint.
- **Playwright seed suite (step-8)** — `tests/d9-step8.spec.ts` (prod-auth project, `test:d9`) automates D9 step-8 T1+T2 against the deployed app; T3 (staff2 create/reset) deferred. Env split required before CI/loops: staff creds in `.env.test` (gitignored, template `.env.test.example`), owner via `.auth/user.json`. Run MANUALLY only — no loops/CI yet. A dedicated testing reference is deferred to the full-suite landing (sequencing decision).

### Supabase migration ledger (verified against prod 7 Jul 2026 via anon-RPC probe)

APPLIED: `20260602_cardless_trial` (inferred — trials work in prod; confirm on next fresh signup), `20260610_player_hub`, `20260610_clubcoins`, `20260615_enable_realtime`, `20260615_topup_intents_coins_credited`, `20260616_pricing_visibility`, `20260617_booking_intents`, `20260618_booking_cancel`, `20260619_booked_slots_rpc`, `20260622_booking_hours_and_per_slot_advance`, `20260625_phase_c_sync_tables`, `20260628_lww_guard`, `20260702_sync_client_fields`, `20260710_phase_d_staff_login` (owner-run 10 Jul; owner regression proven by fresh sign-in + outbox drain; `get_club_subscription_status()` RPC smoke green for BOTH roles in D3).
APPLIED (owner-run via SQL editor ~18–19 Jul; NOT recorded in `supabase_migrations.schema_migrations` — schema-verified 19 Jul via MCP probe: `booking_intents.table_id` = `text`): `20260708_booking_table_id_uuid` (#127 — retypes `booking_intents.table_id` + `submit_booking_intent.p_table_id` + `get_booked_slots.p_table_id` from `int`→`text`; supersedes the old `p_table_id integer` decls in `20260617`/`20260619`).
APPLIED (owner-run via SQL editor ~18–19 Jul; NOT recorded in `supabase_migrations` — schema-verified 19 Jul via MCP probe: `confirm_booking_intent` present; owner E2E-verified → #147 closed): `20260718_booking_pending_expiry` (#147 — 10-min lazy expiry of pending holds in submit/status-poll/confirm, `get_booked_slots` exposes per-slot status, guarded `confirm_booking_intent` RPC with typed `intent_expired`/`slot_taken`, lazy-cleanup predicate fixed to keep future-slot confirmed rows blocking).
APPLIED (owner-run 11 Jul, policies confirmed in `pg_policies`, staff commerce E2E green): `20260710_phase_d6_staff_write_rls_fix` (#130/#131 — staff branches of `wallet_transactions_insert_own_club` + `canteen_items_insert_own_club` rewritten to the wire contract — Pattern S26).
APPLIED (Claude-run 11 Jul via Supabase MCP `apply_migration` — recorded in prod migrations table; policy confirmed in `pg_policies`; E2E green incl. owner-adjustment regression): `20260711_staff_manual_adjustment_rls_exclusion` (#132 — adds `'manual'` to the staff exclusion list; Pattern S26 rule 1 extended).
APPLIED (Claude-run 11 Jul via Supabase MCP; all 9 triggers confirmed in `pg_trigger`; E2E green incl. forge probe): `20260711_server_actor_stamping` (#133 — `zz_stamp_actor`/`zz_stamp_created_by` BEFORE triggers stamp `created_by`/`updated_by` = `auth.uid()` on all 9 synced tables; unforgeable for JWT clients, service-role writes untouched; activates the syncReader equal-ms self-vs-peer tie-break — see ripple_effects §Sync syncReader entry).
APPLIED (Claude-run 19 Jul via Supabase MCP `apply_migration`, recorded in prod migrations table as `20260718202259`; owner could not find it and re-ran the SQL by hand via editor 19 Jul — idempotent `create or replace`, no harm; live def re-verified 19 Jul via `pg_get_functiondef` == migration file; incident produced Rule M): `20260719_reset_club_data_rpc` (#154 — owner-only `reset_club_data()` deletes the club's rows from the 9 synced tables + `booking_intents`/`topup_intents`, children-first, per-table counts returned; `clubs` row + `subscriptions` preserved; staff JWT → 42501; staff `auth.users` accounts OUT of scope by design-omission → #156 scope decision).
APPLIED (Claude-run 19 Jul via Supabase MCP, recorded as `20260719034042`; Rule M probe: `information_schema.columns` has `users_meta.username`, backfill 7/7 rows non-null): `20260719_users_meta_username` (#157 — denormalizes `auth.users.email` onto `users_meta.username` for the owner staff list + copy-login button; supersedes deferred D8b endpoint; `api/create-staff.ts` writes it for new rows).
APPLIED (Claude-run 19 Jul via Supabase MCP, recorded as `20260719034105`; Rule M probe: live `pg_get_functiondef` contains the `removed_staff` purge scoped `um.active = false`): `20260719_reset_purge_removed_staff` (#156 — `reset_club_data()` additionally hard-deletes REMOVED staff `auth.users` rows (cascade → users_meta/profiles) last in the tx, triple-gated club/staff/inactive; active staff + owner + clubs/subscriptions untouched).
APPLIED (Claude-run 19 Jul via Supabase MCP `apply_migration`; Rule M probe: `pg_trigger` has `on_club_created_provision_owner_meta` on public.clubs (tgenabled='O'); post-backfill row check — all 5 clubs `has_meta=true` + `maps_to_own_club=true`): `20260719_provision_owner_users_meta` (#159/#160 — THE cross-device-sync-dead-for-every-customer fix. `AFTER INSERT ON clubs` trigger auto-provisions the owner's `users_meta` row so the JWT gets `user_club_id`; backfilled all pre-trigger owners incl. the new `ball-bended` club; repointed sugeetjadhav@gmail.com from the sugeet21 club to his own clubkeeper club (#160). `users_meta.name` is NOT NULL — sourced from profiles.display_name??email. First apply 23502'd on the missing `name`; v2 fixed. Pattern S28). **Client half (`upsertClub` refreshSession) committed but NOT yet deployed to prod as of this session — existing phones need a token refresh (reopen app) to pick up the claim; verify partners' data uploads after deploy.**
APPLIED (Claude-run 20 Jul via Supabase MCP `apply_migration`; Rule M probe: live `pg_policy` on stock_purchases — both INSERT + UPDATE with_check now `user_role = ANY(owner,staff)`, club-scoped): `20260720_staff_restock_rls` (staff restock — owner-approved. Adds a staff branch to `stock_purchases_insert_own_club` + `stock_purchases_update_own_club` (were owner-only) so `recordStockPurchase`'s `syncedBatch(['stock_purchases','canteen_items'])` staff write no longer 42501/dead-letters. canteen_items UPDATE already allowed staff (D6). Pattern S26 — pending real-staff-restock outbox-drain proof. Bulk-peak stays owner-only, UI-gated only.).
APPLIED (Claude-run 20 Jul via Supabase MCP `apply_migration`; Rule M probe: `information_schema.columns` has `sessions.deleted_by` (uuid) + `sessions.delete_reason` (text)): `20260720_session_reversal_audit` (#162 — additive audit columns for owner session reversal; nullable, no backfill, rides existing sessions RLS).
APPLIED (Claude-run 20 Jul via Supabase MCP `apply_migration`; Rule M probe: `pg_policy` on stock_purchases INSERT+UPDATE with_check now `user_role = ANY(owner,staff)`): `20260720_staff_restock_rls` (owner-approved staff restock — staff branch on stock_purchases so `recordStockPurchase` doesn't 42501; canteen_items UPDATE already allowed staff). **REVERTED 22 Jul by 20260722_revert_staff_restock_rls — see below.**
APPLIED (owner-run via SQL editor 22 Jul; Rule M probe via MCP: `pg_policies` stock_purchases INSERT+UPDATE with_check now `user_role = 'owner'` only, staff branch gone): `20260722_revert_staff_restock_rls` (#169 — restores stock_purchases INSERT+UPDATE to owner-only, reversing 20260720. Paired with Canteen.tsx `<OwnerOnly>` gates on Edit/Delete/Restock. Edit/Delete are UI-gate only — canteen_items UPDATE RLS stays open for the #131 session-decrement path).
APPLIED (owner-run via SQL editor 22 Jul; Rule M probe via MCP: `pg_policies` clubs now has `clubs_staff_select` SELECT on `id::text = user_club_id`, owner select/insert/update intact): `20260722_clubs_staff_select_rls` (#170 — staff can read their `clubs` row so `getOwnerClub()` hydrates `settings.upiId` and the UPI QR renders on staff devices; clubs INSERT/UPDATE stay owner-only; no src change — getOwnerClub relies on RLS, no `.eq()`).
APPLIED (owner-run 23 Jul; Rule M probe via MCP: `pg_proc` confirms `clear_club_sessions()` is SECURITY DEFINER, returns jsonb, takes no args; grants show `authenticated`-only execute, `anon`/`public` revoked): `20260723_clear_club_sessions_rpc` (#155/SWEEP-#154 — owner-gated RPC scoped sibling of `reset_club_data()`; soft-deletes session_items then sessions for the club, `deleted_at`+bumped `updated_at`, idempotent, live-rows-only. Owner-gate rejection of a staff JWT is the one open on-device verification, not MCP-provable — SQL Editor bypasses RLS/has no JWT).
NOT A MIGRATION (verification artifact — shipped alongside for the audit trail, never applied as DDL): `20260723_clear_club_sessions_PROBE.sql` (#155 — the Rule-M `pg_proc`/grants probe queries used to confirm `clear_club_sessions()` is SECURITY DEFINER + `authenticated`-only after owner-run; read-only SELECTs, no schema change).
**Any NEW migration file added under `supabase/migrations/` MUST get a line here (applied or unapplied) in the same session.**

## Open issues — snapshot (GitHub is authoritative; regenerate with `node scripts/sync-state.mjs`)

Hand-notes that survive regeneration: **Phase C sync tail CLOSED by owner 19 Jul — #110/#112/#120/#125/#126 all verified** (outbox S14 mappers, SyncReader tracker, degraded boot, lastVisitAt, write-site cutover). **#97 CLOSED by owner** (PH2 write-order enforced); its sweep spawned #142/#143/#144 (P2, swallowed-MirrorResult, code untouched pending contract decision) + #145 (P1 fresh-device booking-config hydration gap). **#146 CLOSED by owner** (strict-PH2 `updateUpiIdRemote`; both sweep directions now one block on PH2). **Booking v2: D-Booking-1/2 recorded; #147 CLOSED by owner (pending-hold v2 verified; both booking migrations `20260708`+`20260718` live in prod, schema-verified). #148 (code-match polish) filed, code not started. **#153 + #154 + #156/#157/#158 ALL CLOSED by owner 19 Jul** (canonical-phone fix; server-first reset; removed-staff purge + usernames/copy-login + compact rows). Residuals that survive the closes: #153 legacy dual-phone rows need manual merge (canonical wins for new credits); #155 (`clearAllSessions` local-only wipe, SWEEP-#154) SHIPPED to main + deployed 23 Jul (server-first RPC + Dexie mirror), pending owner live-verify. **D-PlayerIdentity-1 recorded (device-token v1; wallet/coins deferred behind verification — owner-mediated pairing leading, WhatsApp OTP fallback; iOS ~7-day ITP eviction accepted). Quartet issue-body drafts presented to owner, NOT filed, no code.** #114 & #121 are duplicates — ask Sugeet to close one. #118 (vacuous tsc build gate) is the biggest regression risk in the repo and needs its own triage session. #138 (residual R5 type debt, ~27 pre-existing baseline errors) pairs with #118. #140 closed (revoke ≤1h JWT-TTL residual access = accepted, not a bug). #136/#137 (staff password + configurable/paid permission tiers) = deferred ENHs. #115 has an RCA comment (DEV-only StrictMode subscriptionLoaded race). **#129 (backfill), #134 (back-entry dropdown), #135 (dup of #139), #139 (auth-lock hang), #141 (/tables Quick-Sale total) CLOSED by owner.** **#168 (NEW this session, P1, code done not pushed) — two concurrent RUNNING sessions on one table; fix = Pattern T11 (dual-layer invariant enforcement + cross-device self-heal). Pending owner live-verify.**

<!-- ISSUES:BEGIN (generated — do not hand-edit between markers) -->
**P0:** #159 Owners never get a users_meta row → JWT has no user_club_id claim → cr.
**P1:** #56 A2 — Subscribe.tsx hardcoded 1500ms delay instead of waiting for webho · #59 A5 — authStore calls openAndSeed on every INITIAL_SESSION re-fire (ris · #61 P2 — PlayerHubSettings handleSaveSlug crashes if clubName is null · #62 W1 — Wallet.tsx has no fetch cancellation on navigate away (setState o · #63 W2 — WalletTopup.tsx UI freezes if db.customers.get() fails after topu · #65 S2 — Settings reset dialog shows stale session count (count read at re · #67 R2 — Realtime initial count never loads if first fetch throws (no erro · #127 Player booking flow broken post-v20: BookingScreen filters tables to n · #145 Fresh device never hydrates acceptsBookings/booking hours/per-slot adv · #160 sugeetjadhav@gmail.com users_meta.club_id points at the WRONG club (su · #165 Walk-in Quick Sales never appear in History (canteenSales table not re · #166 Reverse/delete a walk-in Quick Sale from History (Edit-history toggle).
**P2 / unlabelled:** #55 #57 #58 #60 #64 #66 #102 #113 #114 #115 #118 #119 #121 #123 #136 #137 #138 #142 #143 #144 #148 #149 #150 #151 #152 #155 #161 #162 #163 #164 #167.
<!-- ISSUES:END -->

## Known limitations

- **LIMIT-001 (resolved):** per-user IndexedDB shipped; cross-device sync live (Phase C); write-site cutover 100% (#122/#126/#124).
- **LIMIT-002:** `/api/*` requires `vercel dev` locally; `npm run dev` returns 404 (friendly error in `handlePayNow`).
- **LIMIT-003 (superseded):** the "build sync at 3+ customer asks" threshold was overridden at 2 asks — sync is built.
